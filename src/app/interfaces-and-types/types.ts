export type FilterTypes = 'id'|'company'|'price'|'rating'|'category'|'title' ;

export type SearchFilterTypes = 'company'|'category'|'title' ;

export type buttonColors = 'default'|'default-mix'|'primary'|'accent'|'success'|'warning'|'catalogService.setNewColor()';

export type buttonSize = 'default'|'large'|'small';

export type tooltipPosition = 'above' | 'right' | 'below' | 'left';
``